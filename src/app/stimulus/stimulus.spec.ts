/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Stimulus} from './stimulus';

describe('Stimulus', () => {
  it('should create an instance', () => {
    expect(new Stimulus()).toBeTruthy();
  });
});
